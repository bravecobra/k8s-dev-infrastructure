apiVersion: v1
kind: ConfigMap
metadata:
  name: jaeger-dashboard
  namespace: jaeger
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: /tmp/dashboards/Infrastructure
data:
  {{ (.Files.Glob "files/jaeger-grafana-dashboard.json").AsConfig | nindent 2}}
