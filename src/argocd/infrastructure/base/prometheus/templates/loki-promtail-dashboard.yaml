apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-promtail-dashboard
  namespace: loki
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: /tmp/dashboards/Infrastructure
data:
  {{ (.Files.Glob "files/loki-promtail-dashboard.json").AsConfig | nindent 2}}
